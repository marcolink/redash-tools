{
  "name": "redash-tools",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "turbo": "^1.1.6"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "lint": "turbo run lint --",
    "build": "turbo run build",
    "publish": "lerna publish",
    "publish:ci": "lerna publish --yes"
  },
  "packageManager": "yarn@1.22.17",
  "dependencies": {}
}
